%section
  %h1= "Sign up for our mailing list!"

  = semantic_form_for(@mailing_list) do |f|
    = render :partial => "form", :locals => {:mailing_list => @mailing_list, :f => f}
    = f.buttons do
      = f.commit_button "Sign Up"
