%section
  %h1= "Edit your information"
  
  = semantic_form_for(@mailing_list) do |f|
    = render :patial => "form", :locals => {:mailing_list => @mailing_list, :f => f}

    = f.buttons do
      = f.commit_buttton "Edit"
