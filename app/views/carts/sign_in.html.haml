%section

  %h1= "You must be signed in to place an order"
  .sign-in
    = semantic_form_for @user_session, :url => sign_in_carts_path do |f|
      = f.inputs do
        = f.input :username
        = f.input :password
      = f.buttons do
        = f.commit_button "Submit"


    = link_to "Need an account? Get one here", new_user_path
